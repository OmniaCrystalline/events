/** @format */

import React from "react";
import Current from "../components/currentEvent/Current";

const CurrentPage = () => {
  return (
    <div className='container'>
      <Current />
    </div>
  );
};

export default CurrentPage;
